declare module "enumerate" {
    export const enumerate: (words: string[], cutoff?: number, placeholder?: string) => string;
    export const foo: number;
    export const boo: number;
    export const roo: number;
}
